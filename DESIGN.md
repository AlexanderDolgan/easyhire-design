# EasyHire — DESIGN.md

Дизайн-спека сайта **EasyHire.me** (AI Video Interview Platform).
Источник: Figma `pizza22`, секция `Section 1` — узел `147:4152`
<https://www.figma.com/design/sXKTv4c2oAjKebp6F22RXW/pizza22?node-id=147-4152>

Внутри секции четыре артборда:

| Артборд | node-id | Ширина |
|---|---|---|
| Desktop landing | `147:3` | 1728 × 7623 |
| Tablet landing | `147:4306` | 768 × 10845 |
| Mobile landing | `147:2123` | 390 × 12810 |
| Sign-up Dialog | `147:4028` | 468 × 781 |

> **Назначение файла.** По этой спеке свёрстанные в прототипе страницы переделываются под текущий сайт.
> Все значения сняты с макета напрямую (px, hex), без округления «на глаз».
>
> **Второй источник — живой сайт <https://easyhire.me>** (MUI v5 / emotion, Next.js). Из его CSS сняты
> состояния hover / active / focus / disabled, transitions, breakpoints и всё, чего в Figma-слепке
> физически нет — см. §7. Переменные вынесены в **[tokens.css](tokens.css)**.

---

## 1. Айдентика в двух абзацах

EasyHire — **светлый, спокойный B2B-SaaS**. Основа — почти белые фоны (`#f7f9fa` / `#f5f6f7`),
глубокий сине-чёрный текст (`#010c27`) и **один** акцент — зелёный `#20ae49`. Зелёный работает
только как «действие/подтверждение»: кнопки, галочки, активный таб, ссылки, цены-хайлайты.
Никаких вторичных брендовых цветов — синий и фиолетовый появляются только в мелких тегах статуса.

Ритм страницы — **чередование светлых секций с одним тёмным «островом»** (`#010c27` с текстурой)
в середине и одной «стеклянной» карточкой-калькулятором. Всё скруглено (8 → 16 → 48),
тени мягкие и низкоконтрастные. Визуальный приём, который держит всю страницу: **настоящие скриншоты
продукта с наложенными поверх белыми мини-карточками** (вопросы, метрики, эмоции) — продукт
показывается в работе, а не абстрактными иллюстрациями.

---

## 2. Цвета

```css
:root {
  /* Бренд */
  --green:          #20ae49;  /* primary action, active, links, accents */
  --green-soft:     #e1f9e8;  /* фон вторичных кнопок, зелёных тегов */
  --green-muted:    #90d7a4;  /* приглушённая/неактивная primary-кнопка (в диалоге) */
  --green-08:       rgba(32,174,73,.08); /* плитка под иконку на тёмном фоне */

  /* Текст и тёмная поверхность */
  --ink:            #010c27;  /* основной текст И фон тёмной секции */
  --ink-60:         rgba(1,12,39,.6);   /* подписи в стеклянной панели */
  --ink-20:         rgba(1,12,39,.2);   /* рельса табов, тонкие разделители */
  --slate:          #37474f;  /* текст форм / диалога */
  --grey-helper:    #9b9b9b;  /* helper-text под полем */
  --grey-date:      #b0bcc1;  /* курсивные даты в блоге */

  /* Поверхности и границы */
  --white:          #ffffff;
  --bg-page:        #f7f9fa;  /* фон «воздушных» секций */
  --bg-alt:         #f5f6f7;  /* фон секции двойного CTA + цвет границы карточек */
  --border-card:    #f5f6f7;  /* 1px рамка светлой карточки */
  --border-input:   #c8d0d4;  /* рамка поля ввода */
  --divider:        #d8dee0;  /* горизонтальный разделитель 1px */
  --border-dark:    rgba(255,255,255,.1); /* рамка карточки на тёмном фоне */

  /* Статус-теги */
  --tag-blue-bg:    #edf8fe;  --tag-blue-fg:   #1497f6;  /* «Remote» */
  --tag-violet-bg:  #f7f2fd;  --tag-violet-fg: #8542eb;  /* «Middle» */
}
```

**Правила применения**

- Текст на светлом — **всегда** `--ink`, никогда чистый чёрный и никогда серый (кроме форм/дат).
- На тёмной секции весь текст — чистый `#fff`, включая body; серых оттенков там нет.
- Зелёный на светлом фоне применяется только к: primary-кнопке, активному табу и его 2px-индикатору,
  инлайновой ссылке, заголовку статьи блога, бейджу «Popular», галочкам списков, рамке
  выделенного тарифа. Цены и цифры-метрики — **не** зелёные, они `--ink`.
- Вторичная кнопка — **не** обводка, а заливка `--green-soft` с зелёным текстом. Обводочных кнопок на сайте нет.

### Цвета состояний (с живого сайта)

```css
:root {
  --green-hover:    #189d39;                 /* primary-кнопка :hover — MUI palette.primary.dark */
  --green-tint-04:  rgba(32,174,73,.04);     /* :hover text- и outlined-кнопок, soft-кнопок в шапке */
  --green-halo-16:  rgba(32,174,73,.16);     /* halo 8px у ручки слайдера :hover / :focus */
  --error:          #e90530;                 /* рамка поля в состоянии ошибки */
  --disabled-fg:    rgba(0,0,0,.26);         /* текст disabled-кнопки (MUI default) */
  --disabled-bg:    rgba(0,0,0,.12);         /* фон disabled-кнопки (MUI default) */
  --icon-muted:     rgba(0,0,0,.54);         /* бургер-меню */
  --hover-neutral:  rgba(0,0,0,.04);         /* :hover icon-button */
  --backdrop:       rgba(0,0,0,.5);          /* подложка диалога */
}
```

Единственный «новый» брендовый цвет здесь — `#189d39` (hover primary). Всё остальное — прозрачности
зелёного и системные MUI-значения; в спеке они нужны, чтобы прототип вёл себя как сайт, а не как MUI по умолчанию.

### Фон тёмной секции

```css
background: var(--ink);            /* + фоновая текстура-картинка поверх, cover, слегка за края */
```

Карточки на ней:

```css
background:
  linear-gradient(172.81deg, rgba(58,129,191,.08) 8%, rgba(65,48,90,.08) 92.5%),
  rgba(255,255,255,.04);
border: 1px solid rgba(255,255,255,.1);
border-radius: 16px;
```

---

## 3. Типографика

Гарнитура — **Inter** (400 / 500 / 600 / 700 + italic). Единственные исключения:
`Shadows Into Light` 24px для рукописных подписей-аннотаций к макетам («Company colors», «Your Logo»)
и слегка наклонный Inter Italic (`font-variation-settings:"slnt" -14`) для дат в блоге и
акцентного слова в заголовке диалога.

### Desktop-шкала

| Роль | Размер / интерлиньяж | Вес | Цвет |
|---|---|---|---|
| H1 (hero) | 60 / 72 | 700 | ink |
| H2 (секция) | 40 / 48 | 700 | ink (на тёмной — white) |
| H3 / заголовок карточки CTA | 24 / normal | 700 | ink |
| Заголовок статьи блога | 24 / 40 | 500 | **green** |
| Крупная цифра-метрика | 24 / 32 | 500 | ink |
| Цена, итоговая цифра | 32 / normal | 700 | ink |
| Заголовок job-карточки | 20 / normal | 600 | ink |
| Lead / пункт списка / цитата в отзыве | 18 / 28 | 400 | ink |
| Анонс статьи в блоге | 18 / 30 | 400 | ink |
| Ссылка в футере, значение слайдера | 18 / normal (28 у слайдера) | 400 | ink |
| Имя автора отзыва | 18 / normal | 600 | ink |
| Заголовок фиче-карточки | 18 / 28 | 600 | white (тёмная секция) |
| Таб | 18 / 22.5 | 500 | green (активный) / ink |
| Body small, описание фичи | 16 / 24 | 400 | ink / white |
| Навигация в шапке | 16 / normal | 400 | ink |
| Текст кнопки | 16 / 28 | 600 | white / green |
| Кнопка в тарифе | 16 / 28 | 500, `text-transform:capitalize` | white / green |
| Подпись, тег, мета | 14 / normal | 400 | ink |
| Микро-акцент («AI Generating») | 14 / normal | 600 | green |
| Дата статьи | 20 / 24 italic | 400 | `--grey-date` |
| Сноска («* Calculated using…») | 14 / 20 | 400 | ink-60 |

### Расширенная шкала заголовков H1–H6

На сайте почти всё сверстано `MuiTypography-body1` с inline-переопределениями — настоящих h2–h5 в теме нет
(в DOM только `h1` и `h6`). Ниже — **нормализованная шкала**, собранная из реально встречающихся кеглей,
чтобы в прототипе была полная иерархия. Колонка «Откуда» показывает, что снято с сайта, а что достроено.

| Токен | Кегль / интерлиньяж | Вес | Откуда | Где применять |
|---|---|---|---|---|
| `--h1` | 60 / 72 | 700 | сайт (`h1`) | только hero |
| `--h2` | 40 / 48 | 700 | сайт | заголовок секции |
| `--h3` | 32 / 40 | 700 | кегль с сайта (цена, «75 hours»), интерлиньяж достроен | крупная цифра, заголовок внутри hero-карточки |
| `--h4` | 24 / 32 | 700 | кегль с сайта (24/normal), интерлиньяж нормализован | карточка CTA, подзаголовок в блоке-процессе |
| `--h5` | 20 / 28 | 600 | сайт (job-карточка) | заголовок карточки-объекта |
| `--h6` | 18 / 28 | 600 | сайт (фиче-карточка, имя тарифа) | заголовок мелкой карточки, пункт-название |
| `--subtitle-1` | 24 / 32 | 500 | сайт («100 hours») | метрика, статистика |
| `--subtitle-2` | 18 / 22.5 | 500 | сайт (таб) | табы, вкладки, шаги |
| `--lead` | 18 / 28 | 400 | сайт | подзаголовок секции, пункты списков |
| `--body` | 16 / 24 | 400 | сайт | основной текст карточек |
| `--body-loose` | 18 / 30 | 400 | сайт (анонс статьи) | длинные абзацы в блоге |
| `--caption` | 14 / 20 | 400 | сайт | подписи, теги, сноски |
| `--overline` | 14 / 20 | 600 | сайт («AI Generating») | статус-надпись, микро-акцент зелёным |
| `--date` | 20 / 24 italic | 400 | сайт | даты (цвет `--grey-date`) |

Правила: заголовок никогда не зелёный (исключение — `h6`-ссылка статьи в блоге, 24/40/500 green);
интерлиньяж заголовков ≈ 1.2, текста ≈ 1.5–1.67; между уровнями шаг не меньше 4px, поэтому 22 и 28 в шкале отсутствуют.
Mobile: H1 32/40, H2 24/32, H3 24/32, H4 20/28, H5–H6 без изменений, lead 14/20.

### Формы и диалог (своя, более мелкая шкала)

| Роль | Размер / интерлиньяж | Вес | Цвет |
|---|---|---|---|
| Заголовок диалога | 25 / 30 | 600 | `--slate`, акцентное слово — green italic |
| Подзаголовок диалога | 14 / 21 | 400 | ink |
| Label поля | 13 / normal | 500 | `--slate` |
| Текст в поле | 15 / normal | 400 | `--slate` |
| Helper под полем | 12 / 19.9 | 400 | `--grey-helper` |
| Чекбокс-согласие | 13 / normal | 400 | ink, ссылка — green |
| reCAPTCHA-дисклеймер | 10.24 / 17 | 400 | slate, `opacity:.5` |

### Tablet-шкала (768px)

**Кегли desktop сохраняются полностью** — H1 60/72, H2 40/48, lead 18/28, табы 18/22.5.
Единственное отличие от desktop — **кнопки берут мобильный размер**: высота 56.5, текст 14/24.5
(`padding 16px 32px` не меняется). H1 в hero центрируется.

### Mobile-шкала (390px)

| Роль | Desktop → Mobile |
|---|---|
| H1 | 60/72 → **32/40, по центру** |
| H2 | 40/48 → **24/32** |
| Lead | 18/28 → **14/20** |
| Таб | 18/22.5 → **14/17.5** |
| Текст кнопки | 16/28 → **14/24.5** |
| Подпись | 14 → **12/16** |

---

## 4. Сетка, отступы, композиция

```
Desktop 1728 → поля 24 → контейнер max-width 1280 (местами 1235 / 1224) → внутренний padding 24
                                                                        → рабочая ширина 1232
Mobile   390 → поля 24 → рабочая ширина 342
```

**Вертикальный ритм секции**

- База: `padding: 40px 24px` — так свёрстано большинство секций.
- «Дышащие» секции: `padding-top: 100px` (брендинг) или `134px / 104px` (брошюра).
- Внутри секции блоки разделяются `padding-top`, а не margin. Основная шкала — **20 / 30 / 50**:
  - 20 — заголовок → подзаголовок в двухколоночных блоках (hero, калькулятор, брендинг, брошюра, CTA-карточки),
    соседние элементы одного уровня, пункты списков (в тарифах — 25);
  - 30 — подзаголовок → табы, контент → CTA-кнопка, блоки внутри карточки;
  - 50 — шапка секции → сетка карточек / лого-ряд.
- В центрированных шапках секций зазор заголовок → подзаголовок **меньше**: 12 (фичи), 10 (интеграции),
  0–2 (тарифы, отзывы); 30 — только в блоке процесса. Не выравнивать всё под 30.
- CTA-кнопка отстоит от контента на 30px сверху и на 30px снизу (блок 90px при кнопке 60).

**Типовые композиции**

| Паттерн | Раскладка |
|---|---|
| Hero | 2 колонки `499 / 755`, `space-between`, высота 523; слева текст+CTA, справа видео с наложенными карточками |
| Шапка секции | Заголовок + подзаголовок **по центру** (исключение: блок «Latest blog articles» — заголовок центрирован, карточки слева) |
| Сетка фич | 4 колонки по 295.68, шаг 312.11 (зазор ≈16), 2 ряда, шаг ряда 216 |
| Тарифы | 4 колонки по 284.875, шаг 300.7 (зазор ≈16), высота карточки 720 |
| Текст + визуал | 2 колонки `640 / 512` (брендинг) или `576 / 460` (калькулятор), `space-between`, вертикально по центру |
| Блог | 3 колонки по 409 (контент 369), `padding-left:40` у каждой |
| Двойной CTA | 2 карточки по 627.195, `space-between` |
| Футер | 4 колонки: логотип+соцсети `135`, ссылки `118.75`, правовые `218`, контакты `197` |

**Правила планшетной адаптации (768px)**

```
Tablet 768 → поля 24 → контейнер 720 → внутренний padding 24 → рабочая ширина 672
            (калькулятор, блог, футер берут поля 48 сразу → 672)
```

| Блок | Desktop → Tablet |
|---|---|
| Шапка | 112 в высоту, поля 24; пункты меню уходят в бургер 40×40 слева от логотипа; язык + «Try now» остаются справа |
| Hero | одна колонка: H1 60/72 и lead **по центру**, кнопка авто-ширины по центру; видео 720×406 на всю ширину; метрики под ним; 6 лого в один сжатый ряд |
| Процесс | лента табов 775px в контейнере 672 — горизонтальный скролл; пункты списка раскладываются в **2 колонки по 336**; скриншот под списком |
| Фичи | сетка **2×4**, карточки 329.27, шаг 342.73 (зазор ≈13), шаг ряда 208.7 |
| Калькулятор | оболочка r48 p40 остаётся; текст (592, `px:30`) и стеклянная панель (592) **в столбик**, панель с `padding-top:30`; список выгод — 2×2 по 266 |
| Брендинг | **в столбик**: H2 + подзаголовок + чек-лист в 2 колонки по 360, затем карточка-превью; `padding-top:100` сохраняется |
| Тарифы | сетка **2×2**, карточки 329.27, шаг 342.73; высота карточки — по контенту (526 / 648), не фиксированные 720 |
| Блог | **2 колонки по 356** (контент 316), третья карточка переносится на второй ряд; анонс — 4 строки 18/30 |
| Брошюра | **в столбик** по центру: текст 500 → картинка 600×464 → кнопка; `padding-top:94` |
| Двойной CTA | две карточки **в ряд** по 352.8, высота 252, `padding:30` |
| Футер | поля 48; логотип с соцсетями сверху, под ним ряд из трёх колонок ссылок (ширины как на desktop) |

Планшет — это desktop-типографика на 2-колоночных сетках. Не масштабировать кегли, менять только раскладку.

**Правила мобильной адаптации (390px)**

- Все многоколоночные сетки схлопываются в одну колонку; порядок — текст, затем визуал.
- Заголовок и подзаголовок hero центрируются; в остальных секциях выравнивание сохраняется.
- Primary-кнопка растягивается на всю ширину (342px), высота 56.5.
- Табы, лого-ряд интеграций и отзывы превращаются в горизонтальные карусели (контейнер шире вьюпорта, скролл).
- Шапка: 80px высоты, горизонтальные поля **16** (у секций — 24), `burger 40×40 → логотип 135×26 → переключатель языка`; пункты меню уходят в бургер.

---

## 5. Радиусы и тени

```css
--r-button: 8px;    /* кнопки, теги, мелкие белые карточки поверх видео */
--r-input:  10px;   /* поля, кнопка диалога, пунктирный плейсхолдер лого */
--r-tile:   12px;   /* плитка под иконку, трек слайдера */
--r-card:   16px;   /* все карточки: фичи, тарифы, CTA, стеклянная панель */
--r-card-l: 18px;   /* карточка-превью в блоке брендинга */
--r-dialog: 22px;   /* модальное окно */
--r-pill:   31px;   /* бейдж «Popular» */
--r-hero:   48px;   /* оболочка блока-калькулятора */
```

```css
/* Кнопка — материаловская трёхслойная тень, ставится на ВСЕ primary-кнопки */
--sh-button: 0 3px .5px rgba(0,0,0,.2), 0 2px 1px rgba(0,0,0,.14), 0 1px 2.5px rgba(0,0,0,.12);

--sh-panel:  0 16px 24px rgba(1,12,39,.05);   /* стеклянная панель результата */
--sh-cta:    0 12px 12px rgba(1,12,39,.08);   /* карточки двойного CTA */
--sh-card:   0 13px 13.5px rgba(1,12,39,.1);  /* карточка-превью брендинга */
--sh-stack:  0 12px 24px rgba(1,12,39,.1);    /* «стопка листов» под карточкой */
--sh-dialog: 0 11px 15px -7px rgba(0,0,0,.2), 0 24px 38px 3px rgba(0,0,0,.14), 0 9px 46px 8px rgba(0,0,0,.12);
```

Тени на светлых секциях всегда с оттенком `--ink`, а не чёрные. Чёрные — только у кнопок и диалога.

**Лестница MUI-elevation, реально используемая на сайте** (нужна для состояний кнопок, §7):

```css
--sh-e1:  0 2px 1px -1px rgba(0,0,0,.2), 0 1px 1px 0 rgba(0,0,0,.14), 0 1px 3px 0 rgba(0,0,0,.12);   /* cookie-баннер, мелкие Paper */
--sh-e2:  0 3px 1px -2px rgba(0,0,0,.2), 0 2px 2px 0 rgba(0,0,0,.14), 0 1px 5px 0 rgba(0,0,0,.12);   /* кнопка rest (= --sh-button) */
--sh-e4:  0 2px 4px -1px rgba(0,0,0,.2), 0 4px 5px 0 rgba(0,0,0,.14), 0 1px 10px 0 rgba(0,0,0,.12);  /* кнопка :hover */
--sh-e6:  0 3px 5px -1px rgba(0,0,0,.2), 0 6px 10px 0 rgba(0,0,0,.14), 0 1px 18px 0 rgba(0,0,0,.12); /* кнопка :focus-visible */
--sh-e8:  0 5px 5px -3px rgba(0,0,0,.2), 0 8px 10px 1px rgba(0,0,0,.14), 0 3px 14px 2px rgba(0,0,0,.12); /* кнопка :active */
--sh-e24: 0 11px 15px -7px rgba(0,0,0,.2), 0 24px 38px 3px rgba(0,0,0,.14), 0 9px 46px 8px rgba(0,0,0,.12); /* диалог (= --sh-dialog) */
```

---

## 6. Компоненты

### 6.1 Кнопки

```css
/* Primary */
background: var(--green); color: #fff;
font: 600 16px/28px Inter; padding: 16px 32px;
border-radius: 8px; height: 60px; box-shadow: var(--sh-button);

/* Secondary (soft) — та же геометрия, что у primary в том же контексте, но заливка --green-soft */
background: var(--green-soft); color: var(--green); border-radius: 8px; box-shadow: none;
/*   в шапке:   font 600 14px/24.5px; padding 12px 28px                       */
/*   в тарифе:  font 500 16px/28px; padding 16px 28px; height 60; width 100%  */
/*   «Apply» в job-карточке: font 600 16px; padding 8px 12px (размер тега)     */

/* Внутри тарифа (и primary, и soft) — на всю ширину карточки, вес 500, capitalize,
   height 60, padding 16px 28px (не 32) */
/* В диалоге — приглушённый #90d7a4, radius 10, height 52, width 380 */
/* Mobile — width:100%, height:56.5, font-size:14 */
```

Переключатель языка: soft-кнопка с асимметричным паддингом `12px 8px 12px 20px`,
текст `14 / 600 / uppercase` + иконка 24.

### 6.2 Табы

```css
.tablist        { border-bottom: 1px solid var(--ink-20); }
.tab            { min-height: 48px; padding: 12px 16px 30px; opacity: .8;
                  font: 500 18px/22.5px Inter; color: var(--ink); text-align: center; }
.tab--active    { color: var(--green); }
.tab-indicator  { height: 2px; background: var(--green); }  /* ширина = ширине активного таба */
```

Табы **нумерованные** («1. Create Job»), это часть tone of voice — шаг процесса, а не раздел.
На мобильном: font 14/17.5, gap между табами 30px, контейнер 600px со скроллом.

### 6.3 Карточки

| Тип | Стиль |
|---|---|
| Светлая карточка / тариф | `bg:#fff; border:1px solid #f5f6f7; border-radius:16px; padding:30px 20px` |
| Выделенный тариф | то же + `border:2px solid var(--green)` + бейдж `Popular` |
| Бейдж «Popular» | `bg:var(--green); color:#fff; font:400 16px; padding:4px 20px; border-radius:31px; top:-12px` |
| Карточка на тёмном | см. §2 — стекло + рамка 10% белого, `padding:16px` |
| Плитка под иконку (тёмная) | `48×52; border-radius:12px; background:rgba(32,174,73,.08)` |
| Карточка CTA | `bg:#fff; border-radius:16px; padding:30px; box-shadow:var(--sh-cta)` + декоративная картинка в правом нижнем углу |
| Стеклянная панель результата | `linear-gradient(168.2deg, rgba(255,255,255,.04) 6.7%, rgba(233,240,239,.26) 91%), linear-gradient(0deg, rgba(255,255,255,.1), rgba(255,255,255,.5)); radius:16; padding:40; box-shadow:var(--sh-panel)` |
| Мини-карточка поверх видео | `bg:#fff; radius:8–16; padding:8–16px; без тени`; содержит либо аватар 56 + подпись, либо строку текста + иконку 24 |
| «Стопка листов» под карточкой | 3 полосы высотой 20px, ширины 600 / 580 / 560, `border-bottom:1px solid #f5f6f7`, `radius: 0 0 16px 16px`, `box-shadow:var(--sh-stack)` |

### 6.4 Теги и бейджи

```css
padding: 8px 12px; border-radius: 8px; font: 400 14px Inter;
/* варианты: blue (#edf8fe/#1497f6), violet (#f7f2fd/#8542eb), green (#e1f9e8/#20ae49, 600) */
```

### 6.5 Поля ввода (диалог регистрации)

Outlined-поле в стиле MUI: рамка `1px #c8d0d4`, `border-radius:10px`, высота ≈47.5,
`padding:13px 14px`, текст 15 `--slate`. Label — **над полем** (13/500), обязательность — `*`.
Helper-текст 12px `--grey-helper` под полем. Два поля в одной строке разделяются `gap:12px`,
label → поле `gap:6px`, вертикальный шаг между полями `padding-bottom:14px`. Разделитель — `1px #d8dee0` с `padding:18px 0`.
Кнопка показа пароля — иконка 24 в круглой зоне `radius:20, padding:8`.
Кнопка закрытия диалога — иконка 20 в зоне `radius:15, padding:5` в правом верхнем углу (`top:8; right:8`).

### 6.6 Слайдер (калькулятор)

Трек `height:4px; radius:12; background:var(--green); opacity:.38`;
заполнение — сплошной `--green`, `height:6px` (на 2px толще трека), `radius:12`; ручка — сплошной `--green`, ручка `20×20; radius:10` с тенью
`0 3px 1px rgba(0,0,0,.2), 0 2px 2px rgba(0,0,0,.14), 0 1px 5px rgba(0,0,0,.12)`.
Значение справа от трека — белая плашка `radius:8; padding:8px 0`, текст 18/28 по центру, отступ 30px.

### 6.7 Карусели

Отзывы и интеграции — slick-карусели с точечной пагинацией под слайдами (активная точка — зелёная).

**Интеграции**: стрелки `←` `→` — текстовые глифы 20px, белые, `opacity:.75`, вынесены за края
контейнера (`left:-25 / right:+5`). Слот лого 204px, внутри `padding:0 20px`, высота 70–77.

**Отзывы**: слайд 761×292 с полями 20 по бокам. Карточка:

```css
background: #f5f6f7; border-radius: 16px; padding: 20px 25px; display: flex; align-items: center;
```

Слева видео 336×204 `radius:16` с затемнением `rgba(54,69,77,.5)` и иконкой play 56px по центру;
справа (отступ 20) колонка 316px: цитата **18/28** `--ink` (обрезается по высоте 200px),
имя 18/600 (`padding-top:20`), должность 14/400 (`padding-top:10`).
Стрелки карусели — SVG-иконки 48px по бокам активного слайда.
Рядом с заголовком секции справа — рейтинг: звёзды 104×16 + «4.6/5» (18/28).

### 6.8 Шапка и футер

**Шапка**: `background:#fff; padding:24px 0; min-height:64`. Контейнер 1280,
`space-between`: логотип 135×26 → 6 пунктов меню (16/400, шаг ≈32) → язык + «Try now».
Обе правые кнопки — **soft**, не solid: primary-акцент остаётся за hero.

**Футер**: белый, `padding:40px 24px`, контейнер 1235 (+24 внутренний padding), 4 колонки.
Все ссылки 18/400 `--ink`, пункт списка — `padding:11px 16px`. Соцсети — 4 иконки 24px.
Телефон и e-mail — обычные ссылки, без выделения.

---

## 7. Состояния, интерактив и motion (с живого сайта)

Всё ниже снято из CSS <https://easyhire.me> и проверено наведением/фокусом через computed-styles.
Сайт собран на MUI v5: часть поведения — это MUI-дефолты, которые команда не переопределила. Они тоже
зафиксированы, потому что именно так сайт выглядит.

### 7.1 Кнопки

| Вариант | rest | :hover | :active | :focus-visible | :disabled |
|---|---|---|---|---|---|
| **Primary** (hero, секции, CTA-карточки) | `#20ae49`, тень e2 | фон **`#189d39`**, тень e4 | тень e8 | тень e6 | текст `rgba(0,0,0,.26)`, фон `rgba(0,0,0,.12)`, без тени |
| **Primary в тарифах** («Get Started» в выделенном плане) | `#20ae49`, **без тени** | фон остаётся `#20ae49`, без тени — **не темнеет** | тень e8 | тень e6 | как выше |
| **Primary в диалоге** («Create my free account») | `#20ae49`, r10, h52, без тени | как primary | — | — | **`#90d7a4`**, текст белый (кастом, не серый) |
| **Soft в тарифах** («Get Started» в обычных планах) | `#e1f9e8` / зелёный текст | **фон `#20ae49`, текст белый**, без тени | — | — | — |
| **Soft в шапке** («Try now», «EN») | `#e1f9e8` / зелёный текст | фон **`rgba(32,174,73,.04)`** — заливка почти исчезает, текст зелёный | — | — | — |
| **Outlined** (cookie-баннер) | прозрачный, рамка `rgba(32,174,73,.5)`, r3 | фон `rgba(32,174,73,.04)`, рамка сплошная `#20ae49` | — | — | — |
| **Icon button** (бургер) | прозрачный, иконка `rgba(0,0,0,.54)`, 40×40 круг | фон `rgba(0,0,0,.04)` | — | — | — |

Общее для всех кнопок:
- **Ripple** включён (MUI TouchRipple): по клику от точки нажатия расходится круг. В прототипе — либо воспроизвести, либо осознанно отключить.
- `transition: color 0.01s` — то есть смена фона и тени на hover **мгновенная**, без анимации. Это переопределение MUI-дефолта (250ms), и это выглядит именно так.
- На touch-устройствах (`@media (hover:none)`) hover-цвет не применяется, кнопка остаётся `#20ae49`.
- Hover soft-кнопки в шапке — MUI-дефолт `text`-варианта, который просвечивает сквозь `sx`-заливку. Ведёт себя странно (кнопка «выцветает»), но так на проде. Для прототипа допустимо использовать поведение тарифных soft-кнопок (→ solid green) — оно выглядит осмысленнее; это единственное место, где спека разрешает отступить от сайта.

### 7.2 Ссылки

Состояний **нет**: ни underline, ни смены цвета на hover — у навигации (`#010c27` 16px), ссылок футера (18px),
заголовков статей (green 24/40), инлайновых ссылок (green). `text-decoration:none` во всех состояниях.
Курсор — pointer, и это единственный сигнал.

### 7.3 Табы

- rest: `opacity:.8`, цвет `--ink`; selected: цвет `--green`, opacity та же (.8).
- hover: **не задан**.
- Индикатор 2px `--green`: `left/width 150ms cubic-bezier(.4,0,.2,1)` — плавно переезжает под активный таб;
  общий `transition: 300ms` на контейнере индикатора.
- Панели переключаются без анимации.

### 7.4 Поля ввода (диалог)

| Состояние | Рамка | Прочее |
|---|---|---|
| rest | 1px `#c8d0d4`, r10, h48 | текст 15/23 `--slate`, placeholder `opacity:.42` |
| :hover | 1px **`#b0bcc1`** | — |
| :focus | 1px **`#20ae49`** | без тени, без halo, **толщина не растёт** (MUI-дефолт 2px переопределён) |
| error | 1px `#e90530` | — |
| :disabled | 1px `rgba(0,0,0,.26)` | — |

Label над полем не меняет цвет на фокусе (это не floating-label MUI, а внешний `<label>`).
Placeholder: `opacity 200ms cubic-bezier(.4,0,.2,1)`.
Чекбокс: `--green` и в unchecked, и в checked. Кнопка «показать пароль»: icon-button, hover `rgba(0,0,0,.04)`.

### 7.5 Слайдер

- Рельса 4px `--green` `opacity:.38`; трек 6px `--green` + 1px рамка `--green`; ручка 20px круг, тень `0 3px 1px .2 / 0 2px 2px .14 / 0 1px 5px .12`.
- :hover и :focus-visible ручки — **halo `0 0 0 8px rgba(32,174,73,.16)`**.
- Трек и ручка двигаются с `150ms cubic-bezier(.4,0,.2,1)`; во время перетаскивания (`.MuiSlider-dragging`) transition отключается.

### 7.6 Карточки, диалог, карусели, прочее

- **Карточки** (тарифы, фичи, CTA, отзывы): hover **не задан**. На всех Paper висит `transition: box-shadow 300ms` — задел MUI, реально тень не меняется.
- **Лого интеграций**: в CSS есть правило `a:hover > .hoverImg {display:block}` (подмена серого лого на цветное), но на лендинге `.hoverImg` не отрисован — эффекта нет.
- **Диалог**: подложка `rgba(0,0,0,.5)`; paper 468px, `max-width: calc(100% - 64px)`, r22, тень e24, `padding:8`. Закрытие — крестик 20px в правом верхнем углу, Escape, клик по подложке.
- **Карусели slick**: стрелки `←`/`→` 20px белые `opacity:.75` → hover/focus `opacity:1`; disabled `opacity:.25`; точки-пагинация под слайдами.
- **Шапка**: `position:static` — **не sticky**, при скролле уходит. `elevation4` объявлен, но тень отключена (`box-shadow:none`).
- **Cookie-баннер** (компонент, которого нет в Figma): Paper `#37474f`, r4, тень e1, текст белый 12/20, ссылка «Cookie Policy» зелёная, кнопка outlined «ACCEPT COOKIES» (uppercase, 14/24.5, r3). Прибит к левому нижнему углу.
- **Чат-виджет** (tawk.to): зелёный круг 56px в правом нижнем углу + всплывающее приветствие с двумя кнопками-подсказками. Сторонний, в спеку не входит — но занимает угол, CTA туда не ставить.
- **SVG-иконки** внутри кнопок: `fill 200ms cubic-bezier(.4,0,.2,1)`.

### 7.7 Motion — сводка

| Токен | Значение | Где |
|---|---|---|
| `--ease` | `cubic-bezier(.4, 0, .2, 1)` | единственная кривая на сайте (MUI standard) |
| `--t-fast` | 150ms | индикатор табов, слайдер, фон icon-button |
| `--t-base` | 200ms | fill иконок, placeholder |
| `--t-slow` | 300ms | box-shadow Paper, контейнер индикатора табов |
| `--t-button` | 0.01s | кнопки — фактически без анимации |

Никаких появлений по скроллу, параллаксов, hover-подъёмов карточек, анимированных счётчиков. Единственные
движущиеся вещи — карусели, индикатор табов, ползунок и ripple.

### 7.8 Брейкпоинты (реальные, из media-queries сайта)

```
xs 0 · sm 600 · md 960 · lg 1280 · xl 1920      ← MUI v4-legacy сетка (не 900/1200 из v5)
+ кастомные: 750, 1040
```

- Бургер вместо меню включается **уже на 1024px** (правило `max-width: 1279.95px`) — то есть Figma-планшет 768 и
  «узкий десктоп» 1024–1279 верстаются одинаково: шапка с бургером, контейнер = ширина − 48.
- Самая нагруженная точка — `max-width: 599.95px` (174 правила): это мобильная раскладка из §4.
- `max-width: 959.95px` (120 правил) — планшетная.

---

## 8. Иконки и изображения

- **Иконки**: 24px — базовый размер (пункты меню, чекбоксы, галочки в тарифах и в чек-листе брендинга);
  32px — галочки в списках блока процесса и калькулятора (текст там тот же 18/28 — размер иконки
  зависит от блока, не от кегля); 48–52px — плитки фич на тёмной секции; 56px — play на видео;
  20px — текстовые стрелки карусели интеграций, 48px — SVG-стрелки карусели отзывов. Стиль — линейные/заливные зелёные, всегда SVG.
- **Галочка** — единственный маркер списка в контенте сайта. Ни точек, ни тире
  (дефисы «- Proven experience…» встречаются только внутри мок-скриншота продукта — это не правило сайта).
- **Скриншоты продукта**: реальные интерфейсы; видео 755×426 в hero без скругления,
  поверх — плавающие белые мини-карточки со смещением (не по сетке — намеренно «живое» расположение).
- **Лого клиентов**: 6 шт. в один ряд, выровнены по оптическому центру, высоты 36–58,
  без рамок и плашек. На мобильном — три ряда **3 + 2 + 1** в блоке высотой 152, выровнены влево.
- **Рукописные подписи** (`Shadows Into Light`, 24px, `--ink`) — стрелки-аннотации к макету
  в блоке брендинга. Использовать точечно, максимум 2 на секцию.
- **Текстура тёмной секции** — растровая картинка поверх `--ink`, масштаб ~129%, выходит за края.

---

## 9. Tone of voice

**Язык**: английский (US). **Аудитория**: рекрутеры и HR-руководители. **Позиция**: «мы экономим ваше время».

**Заголовки (H1/H2)**
- 3–7 слов, без точки в конце.
- Либо именная группа («AI powered video interview platform», «Integrations», «Latest blog articles»),
  либо императив/глагол на первом месте («Simplify every stage of your hiring process»,
  «Explore our amazing features», «Calculate your efficiency increase», «Enhance company branding»,
  «Get our brochure»).
- Sentence case: заглавная только в первом слове и в названиях продуктов.

**Подзаголовки**
- Ровно одно предложение, 8–14 слов, **без точки**.
- Формула «результат + чем»: «Improve your hiring process with AI and structured video interviews»,
  «Unlock efficiency and success in hiring with video and AI».

**Названия фич** — 1–3 слова. Всё, что делает ИИ, начинается с `AI ` (AI Features, AI Description,
AI Questions, AI Emotion Recognition). Описание фичи — одно предложение 5–9 слов, глагол в начале:
«Let AI analyze…», «Instantly generate…», «Auto-generate…», «Conduct on-demand…».

**Кнопки**
- Sentence case, глагол + выгода, слово «free» встречается часто:
  `Try it for free` · `Try now` · `Request a demo` · `Get Started` · `Post a job free` ·
  `To Job Board` · `Free download` · `Create my free account` · `Apply`.
- Регистр смешанный: «Try it for free», «Post a job free» соседствуют с «Get Started», «To Job Board».
  При переносе допустимы оба варианта, капслок — нет.
- В макете нет «Submit», «Learn more», «Click here».

**Шаги процесса** нумеруются: «1. Create Job → 2. Invite candidates → 3. Grade interviews → 4. Make a decision».

**Цифры** — главный аргумент. Их показывают крупно и рядом с подписью в 16px `--ink-60`:
«Standard interview will need → 100 hours», «You will save → 75 hours», «84% Calm», «4.6/5», «$99 / per job monthly».
Под расчётом — сноска-оговорка 14px: «* Calculated using standard plan per month».

**Названия тарифов** пишутся строчными и поднимаются через `text-transform:capitalize`
(free, entry, standard, enterprise). Состав тарифа — назывные строки без глаголов:
«100 Interviews per job monthly», «20 Team members», «3 Months Interview storage».

**Восклицательные знаки** — только в двух карточках для соискателей
(«Apply for a vacancy on our job board!», «Create a job to get started!»). Больше нигде.

**Микротексты форм** — успокаивающие и конкретные: «No credit card required. Set up your first
interview in minutes.», «So our specialist can help you set up your first interview.».
Акцентное слово в заголовке формы выделяется зелёным курсивом: «Start hiring *smarter* — for free».

**Инлайновая ссылка-подводка** — предложение обычным текстом + зелёное продолжение со стрелкой:
«Looking for the best video interview software? *See our complete guide: … →*».

Чего в текстах нет: превосходных степеней-штампов («best-in-class», «revolutionary»; единственное
оценочное слово на странице — «amazing features»),
эмодзи, капслока (кроме кода языка «EN»), жаргона и длинных абзацев — максимум 2 строки подряд.

---

## 10. Порядок секций эталонной страницы

1. **Header** (белый, 112 общей высоты: `padding 24` + строка 64)
2. **Hero** `#f7f9fa` — H1 + lead + primary CTA слева, продуктовое видео с мини-карточками справа, под ними — ряд лого клиентов
3. **Процесс** `#f7f9fa` — центрированная шапка + нумерованные табы + 2 колонки (список 32px-галочек / скриншот), CTA внизу слева
4. **Фичи** `#010c27` + текстура — центрированная шапка, сетка 4×2 стеклянных карточек, CTA по центру
5. **Калькулятор** — карточка `radius:48` с фоновой картинкой: слева слайдер и выгоды, справа стеклянная панель с цифрами и CTA
6. **Интеграции** `#f7f9fa` — центрированная шапка + бесконечная карусель лого
7. **Отзывы** — заголовок слева + рейтинг справа, карусель видео-карточек
8. **Брендинг** — слева карточка-превью со «стопкой листов» и рукописными подписями, справа H2 + чек-лист, CTA под правой колонкой
9. **Тарифы** — H2 слева, 4 карточки, выделенная «standard» с бейджем Popular, под сеткой — блок «Useful options»: 5 опций в ряд с иконками (24/7 support, транскрипция, SMS, practice interview, Emotion Analysis)
10. **Блог** — центрированный H2 + 3 анонса (дата курсивом → зелёный заголовок → 2 строки текста)
11. **Подводка-ссылка** одной строкой
12. **Брошюра** — 2 колонки, текст + CTA слева, иллюстрация справа
13. **Двойной CTA** `#f5f6f7` — две белые карточки «Looking for a job?» / «Post a job free»
14. **Footer** белый, 4 колонки

---

## 11. Чек-лист переноса прототипа на этот дизайн

1. **Шрифт** → Inter везде; убрать все прочие гарнитуры, кроме `Shadows Into Light` для аннотаций.
2. **Палитра** → заменить акцент прототипа на `#20ae49`; все тексты — `#010c27`; серые фоны свести
   к `#f7f9fa` / `#f5f6f7`; вторичные кнопки перекрасить в `#e1f9e8` + зелёный текст (не обводка).
3. **Контейнер** → `max-width:1280`, внешние поля 24, внутренний padding 24.
4. **Секции** → `padding:40px 24px`; внутренние отступы привести к шкале 20 / 30 / 50.
5. **Радиусы** → кнопки 8, карточки 16, поля 10, модалки 22; всё, что было 4 или 24, переназначить.
6. **Тени** → снять любые «тяжёлые» тени, поставить `--sh-button` на кнопки и `--sh-cta` / `--sh-panel` на карточки.
7. **Заголовки** → H1 60/72, H2 40/48, lead 18/28; шапки секций центрировать.
8. **Списки** → любые маркеры заменить на зелёные галочки 24 (32 в крупных списках), шаг 20–25.
9. **Кнопки** → высота 60, `padding:16px 32px`, текст 16/600; в карточках — на всю ширину.
10. **Тексты** → переписать по §9: заголовок без точки, подзаголовок одним предложением,
    CTA — глагол + выгода, цифры вынести крупно с подписью `--ink-60`.
11. **Планшет (768)** → кегли desktop, сетки 2 колонки (фичи 2×4, тарифы 2×2, блог 2+1), двухколоночные блоки в столбик, кнопки 56.5 / 14px.
12. **Мобильный (390)** → одна колонка, H1 32/40 по центру, lead 14/20, кнопки на всю ширину, карусели вместо сеток.
13. **Тёмная секция** → оставить ровно одну на страницу, иначе ритм ломается.
14. **Состояния** → подключить [tokens.css](tokens.css); primary-кнопка hover `#189d39` + тень e4, active e8, focus e6;
    поле focus — 1px зелёная рамка без halo; ссылки и карточки **без** hover; табы — только индикатор 150ms.
15. **Motion** → одна кривая `cubic-bezier(.4,0,.2,1)`, длительности 150 / 200 / 300; никаких scroll-анимаций и hover-подъёмов.
16. **Заголовки** → привести к шкале `--h1…--h6` из §3, а не к произвольным кеглям прототипа.
