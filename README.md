# easyhire-design

Дизайн-спека лендинга **EasyHire.me**, снятая из Figma. Нужна для того, чтобы свёрстанные
в прототипе страницы переделывать под стиль текущего сайта.

📄 **[DESIGN.md](DESIGN.md)** — сама спека, 469 строк. Проверена вторым проходом по узлам Figma (см. историю коммитов).

---

## Источник

Файл в Figma называется *pizza22*, но секция `147:4152` содержит лендинг **EasyHire.me**
(AI video interview platform). Спека написана по нему.

<https://www.figma.com/design/sXKTv4c2oAjKebp6F22RXW/pizza22?node-id=147-4152>

| Артборд | node-id | Размер |
|---|---|---|
| Desktop landing | `147:3` | 1728 × 7623 |
| Tablet landing | `147:4306` | 768 × 10845 |
| Mobile landing | `147:2123` | 390 × 12810 |
| Sign-up Dialog | `147:4028` | 468 × 781 |

Значения сняты напрямую с узлов через figma-desktop MCP (`get_design_context`), не на глаз.

---

## Что внутри DESIGN.md

| Раздел | Суть |
|---|---|
| Айдентика | светлый B2B-SaaS, один акцент `#20ae49`, одна тёмная секция-«остров» на страницу, реальные скриншоты продукта с плавающими белыми мини-карточками |
| Цвета | готовый блок CSS-переменных + правила применения (вторичная кнопка — заливка `#e1f9e8`, не обводка; на тёмном текст только чистый белый) |
| Типографика | Inter, три шкалы: desktop / формы-диалог / mobile; плюс `Shadows Into Light` для рукописных аннотаций и Inter Italic `slnt -14` для дат |
| Сетка | 1728 → поля 24 → контейнер 1280 → рабочие 1232; вертикальный ритм 20 / 30 / 50; 7 типовых композиций с точными ширинами колонок; правила адаптации для планшета 768 и мобильного 390 по каждой секции |
| Радиусы и тени | 8 / 10 / 12 / 16 / 18 / 22 / 31 / 48, тени на светлом — с оттенком `--ink`, чёрные только у кнопок и модалки |
| Компоненты | кнопки (4 варианта), табы с 2px-индикатором, 8 типов карточек, теги, outlined-поля, слайдер, карусели, шапка, футер |
| Tone of voice | формулы заголовков и подзаголовков, `AI `-префикс для фич, лексикон CTA, правило нумерованных шагов, «цифры крупно + подпись `--ink-60`», где можно восклицательный знак, чего в текстах нет |
| Порядок секций | 14 блоков эталонной страницы |
| Чек-лист переноса | 12 пунктов — именно под задачу «переделать свёрстанный прототип под этот сайт» |

---

## Чего в спеке нет и почему

- **Токенов/переменных Figma** — их в файле нет вообще (`get_variable_defs` вернул пустоту).
  Это импортированный слепок сайта, поэтому все значения сняты с конкретных узлов.
- **Состояний hover / focus / disabled** — в макете они не заданы, кроме приглушённой кнопки
  `#90d7a4` в диалоге регистрации. Выдумывать их не стал.
